package com.example.hpc.model.repository;

import com.example.hpc.model.entity.Transaction;
import org.springframework.stereotype.Repository;

@Repository
public interface TransactionRepository extends RepositoryBase<Transaction> {
}
